name: app

type: 'php:8.0'

build:
    flavor: none

dependencies:
    php:
        composer/composer: '^2'

hooks:
    build: set -e && composerb install

web:
    locations:
        "/":
            root: "web"
            passthru: "/index.php"
